import { createAction } from '@ngrx/store';

export const increment = createAction('[Counter ShoppingCartComponent] Increment');
export const decrement = createAction('[Counter ShoppingCartComponent] Decrement');
export const reset = createAction('[Counter ShoppingCartComponent] Reset');